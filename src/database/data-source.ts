// Migration system removed — schema is now managed by TypeORM's `synchronize: true`
// (see src/app.module.ts). This file is inert and safe to delete by hand; the sandbox
// this was edited from can't delete files on your filesystem, only empty them.
export {};
