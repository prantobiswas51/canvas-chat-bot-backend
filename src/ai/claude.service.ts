// Removed — the app now only calls Gemini directly (see gemini.service.ts).
// This file is kept as an empty placeholder because the sandbox can't
// delete files on this mount; nothing imports it anymore. Safe to delete
// by hand next time you're editing on your machine.
export {};
